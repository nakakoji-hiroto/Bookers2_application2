<div class="container">
  <div class="row">
    <div class='mx-auto col'>
      <h2>Follower Users</h2>
      <table class="table">
        <tr>
          <td>image</td>
          <td>name</td>
          <td colspan="3"></td>
        </tr>
        <tr>
          <td><%= image_tag @user.get_profile_image, size: '50x50' %></td>
          <td><%= @user.name %></td>
          <td>フォロー数：<%= @follower.count %></td>
          <td>フォロワー数：<%= @followed.count %></td>
          <td><%= link_to 'Show', user_path(@user) %></td>
        </tr>
      </table>
    </div>
  </div>
</div>